define(["jquery", "react", "reactDOM"], function(jquery, React, ReactDOM) {
    $(function() {
        console.log("Loaded in app");
        console.log(jquery);
        console.log(React);
        console.log(ReactDOM);
    });
});